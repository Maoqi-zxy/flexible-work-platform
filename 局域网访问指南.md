# 🔧 局域网访问问题排查指南

## 当前状态

| 项目 | 状态 |
|------|------|
| **前端服务** | ✅ 运行中 (端口 3001) |
| **后端服务** | ✅ 运行中 (端口 3000) |
| **监听地址** | ✅ 0.0.0.0 (所有网络接口) |
| **防火墙** | ✅ 已关闭 |
| **本机访问** | ✅ 正常 |

---

## 在其他设备上测试

### 步骤 1：确认在同一 WiFi

确保其他设备已连接到**同一个 WiFi 网络**

### 步骤 2：测试网络连通性

**在手机/其他电脑的浏览器中访问：**
```
http://30.174.145.97:3001/
```

**或使用命令行测试：**
```bash
# Windows (命令提示符)
ping 30.174.145.97
curl http://30.174.145.97:3001/

# Mac/Linux
ping 30.174.145.97
curl http://30.174.145.97:3001/

# 手机可以使用 Ping 工具 App
```

### 步骤 3：如果无法访问

#### 可能原因 1：路由器 AP 隔离
- **症状**：设备能上网但不能互访
- **解决**：登录路由器后台，关闭"AP 隔离"或"客户端隔离"

#### 可能原因 2：公司网络限制
- **症状**：公司 WiFi 通常禁止设备互访
- **解决**：使用手机热点测试，或部署到公网

#### 可能原因 3：IP 地址变化
- **检查**：在 Mac 终端运行：
  ```bash
  ifconfig en0 | grep "inet "
  ```
- 确保其他设备访问的 IP 与显示的一致

---

## 快速解决方案

### 方案 A：使用内网穿透（推荐）

使用 ngrok 获得公网 URL：

```bash
# 安装 ngrok
brew install ngrok

# 启动前端穿透
ngrok http 3001

# 启动后端穿透（新终端窗口）
ngrok http 3000
```

会生成类似这样的公网 URL：
- 前端：`https://xxx-xxx.ngrok.io`
- 后端：`https://xxx-xxx.ngrok.io`

**优点**：任何地方都能访问
**缺点**：免费版每次重启 URL 会变

### 方案 B：部署到公网（最佳）

已配置好的部署方案：

```bash
# 前端 - Vercel 部署
cd /Users/yu/.homiclaw/workspace-agent-p92uww/flexible-work-platform
npx vercel --prod

# 后端 - Railway 部署
访问 https://railway.app 连接 GitHub 仓库
```

### 方案 C：使用 Mac 热点

1. Mac 开启个人热点
2. 其他设备连接 Mac 的热点
3. 访问 `30.174.145.97:3001`

---

## 临时测试：手机扫码访问

如果你需要快速测试，可以：
1. 在 Mac 浏览器打开 `http://localhost:3001/`
2. 使用 Mac 的"隔空投送"或微信传到手机
3. 或在手机上手动输入地址

---

## 需要进一步帮助？

请告诉我：
1. 其他设备能否 `ping 30.174.145.97`？
2. 是什么类型的设备？（Windows/Mac/手机）
3. 在公司网络还是家庭网络？